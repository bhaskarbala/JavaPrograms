package arrays;

public class ArraysReffance {
	// Whata is Arrays
	/*
	 *  Arrays are store the smiler data must should be homegezens and size fixed 
	 *  we have to store  the data for based on index 
	 */
	//  retrival from the Array  we have 3 approcha 
	/*
	 * system.out.println(a[o]);
	 * for each loop
	 * for loop;
	 */
}
